#!/usr/bin/env bash

git config --global user.name "LauraLundbye"
git config --global user.email "202106724@post.au.dk" # adding git credentials 🙂
# to run make sure you are in the correct folder in terminal and write bash setup.sh